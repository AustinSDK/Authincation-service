<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <%- include('partials/nav', { user: user }) %>
    <main class="container">
        <header>
            <h1>Welcome, <%= user.username %>!</h1>
            <p style="margin-top:.25rem;" class="muted">Access the tools available to your role below.</p>
        </header>
        <section>
            <h2>Tools</h2>
            <ul class="project-list" style="margin-top:1rem;">
            <% projects.forEach(element => { %>
                <li class="project-card">
                    <a class="title" href="<%= element.link %>"><%= element.name %></a>
                    <div class="muted"><%= element.description %></div>
                </li>
            <% }) %>
            </ul>
        </section>
    </main>
    

</body>
</html>